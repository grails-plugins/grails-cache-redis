The Grails Cache Redis plugin extends the [Cache|http://grails.org/plugin/cache] plugin and uses [Redis|http://redis.io/] as the storage provider for cached content. It replaces the core plugin's @grailsCacheManager@ bean with one that uses a Redis backend.